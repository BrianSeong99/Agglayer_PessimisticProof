[package]
name = "methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = { version = "1.2.4", default-features = false, features = ['unstable'] }
puffin = "0.19.1"
nvtx = "1.3.0" 

[package.metadata.risc0]
methods = ["pp-risc0-guest"]
